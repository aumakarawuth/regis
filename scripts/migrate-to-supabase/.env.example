> cp this to .env and fill in real values — never commit .env

SHEET_ID=1Jji1XE5-pRezwFAf2z18AUXGDAwEGsALO6u84iDi_2E
GOOGLE_SERVICE_ACCOUNT_KEY_FILE=./service-account.json

SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
# Project Settings -> API -> service_role key. This bypasses RLS —
# required so the script can write rows as "system", not as anon/admin.
# Never expose this key client-side.
SUPABASE_SERVICE_ROLE_KEY=eyJ...
